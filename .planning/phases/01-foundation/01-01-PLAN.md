---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - .cargo/config.toml
  - wsl-core/Cargo.toml
  - wsl-core/src/lib.rs
  - wsl-core/src/error.rs
  - wsl-core/src/config.rs
  - wsl-tui/Cargo.toml
  - wsl-tui/src/main.rs
  - wsl-web/Cargo.toml
  - wsl-web/src/main.rs
autonomous: true
requirements:
  - FOUND-01
  - FOUND-04
  - FOUND-07
  - FOUND-10
  - DX-03
  - DX-07

must_haves:
  truths:
    - "`cargo build --workspace` completes with zero errors on Windows 11"
    - "`cargo clippy --workspace` completes with zero warnings on Windows 11"
    - "Config loads from `~/.wsl-tui/config.toml` with sensible defaults when no file exists"
    - "`WSL_TUI_STORAGE` env var overrides the storage setting from config.toml"
    - "First run auto-creates `~/.wsl-tui/` directory"
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace manifest with resolver v2, workspace.dependencies, workspace.package"
      contains: "resolver = \"2\""
    - path: ".cargo/config.toml"
      provides: "Windows stack size linker flag for libsql"
      contains: "STACK:8000000"
    - path: "wsl-core/src/error.rs"
      provides: "CoreError enum with thiserror derive"
      contains: "thiserror"
    - path: "wsl-core/src/config.rs"
      provides: "Config struct with TOML loading, env overrides, StorageMode enum"
      exports: ["Config", "StorageMode"]
    - path: "wsl-core/src/lib.rs"
      provides: "Library crate root re-exporting config and error modules"
  key_links:
    - from: "wsl-tui/Cargo.toml"
      to: "wsl-core"
      via: "workspace dependency"
      pattern: "wsl-core.*path"
    - from: "wsl-core/src/config.rs"
      to: "wsl-core/src/error.rs"
      via: "CoreError for config loading failures"
      pattern: "CoreError"
---

<objective>
Create the Cargo workspace scaffold with all three crates compiling cleanly, the Windows linker flag in place, and a complete config + error system in wsl-core.

Purpose: Establish the compilable foundation that every subsequent plan depends on. Solve the libsql stack overflow at the linker level before any storage code is written. Deliver the config system so Plans 02 and 03 can consume storage mode and config values.

Output: Compilable 3-crate workspace with config loading, error types, and zero clippy warnings.
</objective>

<execution_context>
@C:/Users/mikkel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mikkel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Cargo workspace with all crate stubs and Windows linker flag</name>
  <files>
    Cargo.toml
    .cargo/config.toml
    wsl-core/Cargo.toml
    wsl-core/src/lib.rs
    wsl-tui/Cargo.toml
    wsl-tui/src/main.rs
    wsl-web/Cargo.toml
    wsl-web/src/main.rs
  </files>
  <action>
    Create the workspace root `Cargo.toml` with:
    - `[workspace]` table: `members = ["wsl-core", "wsl-tui", "wsl-web"]`, `resolver = "2"`
    - `[workspace.package]`: version "0.1.0", edition "2021", rust-version "1.88", authors ["Mikkel Georgsen"], license "MIT", repository "https://github.com/mikl0s/wsl-tui"
    - `[workspace.dependencies]`: All dependencies from 01-RESEARCH.md Standard Stack section (ratatui 0.30, crossterm 0.29 with event-stream, tokio 1 with full, serde 1 with derive, serde_json 1, toml 0.8, thiserror 2, anyhow 1, dirs 6, encoding_rs 0.8, libsql 0.9 with default-features=false and features=["core"], futures 0.3, async-trait 0.1)
    - `[profile.release]`: opt-level "z", lto "fat", codegen-units 1, strip true

    Create `.cargo/config.toml` with:
    - `[target.x86_64-pc-windows-msvc]` rustflags = ["-C", "link-arg=/STACK:8000000"]
    - `[target.x86_64-pc-windows-gnu]` rustflags = ["-C", "link-arg=-Wl,--stack,8000000"]
    - Comment referencing GitHub issue #1051

    Create `wsl-core/Cargo.toml`:
    - `[package]` inheriting workspace.package fields (version.workspace, edition.workspace, etc.)
    - Dependencies: thiserror, serde, serde_json, toml, dirs, encoding_rs, tokio (workspace = true for all), libsql (workspace = true), async-trait (workspace = true), anyhow (workspace = true)

    Create `wsl-tui/Cargo.toml`:
    - Dependencies: wsl-core (path = "../wsl-core"), ratatui, crossterm, tokio, anyhow, futures (all workspace = true)

    Create `wsl-web/Cargo.toml`:
    - Dependencies: wsl-core (path = "../wsl-core"), tokio, anyhow (all workspace = true)

    Create stub source files:
    - `wsl-core/src/lib.rs`: empty module declarations (will be populated in Task 2)
    - `wsl-tui/src/main.rs`: `#[tokio::main] async fn main() -> anyhow::Result<()> { Ok(()) }`
    - `wsl-web/src/main.rs`: `#[tokio::main] async fn main() -> anyhow::Result<()> { println!("wsl-web: not yet implemented"); Ok(()) }`

    Run `cargo build --workspace` and `cargo clippy --workspace` to verify zero errors and zero warnings.
  </action>
  <verify>
    Run `cargo build --workspace` — must exit 0 with no errors.
    Run `cargo clippy --workspace -- -D warnings` — must exit 0 with no warnings.
    Verify `.cargo/config.toml` contains the STACK:8000000 flag.
  </verify>
  <done>
    All three crates compile cleanly. Clippy reports zero warnings. The Windows linker flag is in place for both MSVC and GNU targets. Release profile is configured for binary size optimization.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create error types and config system with env overrides and fully commented default</name>
  <files>
    wsl-core/src/error.rs
    wsl-core/src/config.rs
    wsl-core/src/lib.rs
  </files>
  <action>
    Create `wsl-core/src/error.rs`:
    - Define `CoreError` enum with `#[derive(Debug, thiserror::Error)]`
    - Variants: `NoHomeDir`, `ConfigParse(String)`, `ConfigRead(#[from] std::io::Error)`, `TomlParse(#[from] toml::de::Error)`, `StorageError(String)`, `WslExec(String)`, `WslFailed(String)`, `PluginError(String)`
    - Add display messages for each variant
    - Write unit tests for error Display formatting

    Create `wsl-core/src/config.rs`:
    - Define `StorageMode` enum: `Auto`, `Libsql`, `Json` with `Deserialize`, `Default` (default = Auto), and `FromStr` impl for env var parsing
    - Define `Config` struct with `#[derive(Debug, Deserialize, Default)]`:
      - `storage: StorageMode` (default Auto)
      - `config_dir: PathBuf` (computed, not deserialized — set to `~/.wsl-tui/`)
      - `first_run: bool` (computed, not deserialized — true if config.toml didn't exist)
    - Implement `Config::load() -> Result<Self, CoreError>`:
      1. Resolve config dir via `dirs::home_dir().join(".wsl-tui/")`
      2. Create dir with `fs::create_dir_all` if missing
      3. Check if `config.toml` exists — if not, write the fully commented default (see below) and set `first_run = true`
      4. Parse config.toml via `toml::from_str`
      5. Apply `WSL_TUI_*` env var overrides: `WSL_TUI_STORAGE` -> `config.storage`
      6. Set `config_dir` on the struct
    - Create a `DEFAULT_CONFIG_TOML` const string with ALL available options commented out with descriptions. It should read like good documentation:
      ```toml
      # WSL TUI Configuration
      # Location: ~/.wsl-tui/config.toml
      #
      # All settings below show their default values.
      # Uncomment and modify to customize.

      # Storage backend: "auto" (try libsql, fall back to json), "libsql", or "json"
      # Can also be set via WSL_TUI_STORAGE environment variable
      # storage = "auto"
      ```
      Include placeholder sections for future phases (connection, theme, keybindings) as commented headers only.
    - Write unit tests:
      - `test_default_config()` — Config::default() has storage = Auto
      - `test_storage_mode_from_str()` — parse "auto", "libsql", "json", invalid
      - `test_env_override()` — set WSL_TUI_STORAGE, verify it takes precedence
      - `test_config_dir_creation()` — verify config dir is created (use tempdir)

    Update `wsl-core/src/lib.rs`:
    - Declare `pub mod config;` and `pub mod error;`
    - Re-export key types: `pub use config::Config;` and `pub use error::CoreError;`

    Run `cargo test --workspace` and `cargo clippy --workspace`.
  </action>
  <verify>
    Run `cargo test -p wsl-core` — all config and error tests pass.
    Run `cargo clippy --workspace -- -D warnings` — zero warnings.
    Verify `Config::load()` creates `~/.wsl-tui/` and writes a commented config.toml on first run (test with tempdir).
  </verify>
  <done>
    CoreError enum exists with all Phase 1 variants. Config struct loads from TOML with env var overrides. Fully commented default config.toml is written on first run. All public functions have unit tests. Clippy clean.
  </done>
</task>

</tasks>

<verification>
1. `cargo build --workspace` exits 0
2. `cargo clippy --workspace -- -D warnings` exits 0
3. `cargo test -p wsl-core` passes all tests
4. `.cargo/config.toml` contains `/STACK:8000000`
5. `Cargo.toml` contains `resolver = "2"`
6. Config system handles missing file, env overrides, and dir creation
</verification>

<success_criteria>
- Three-crate workspace compiles with zero errors and zero warnings
- Windows stack size linker flag is in `.cargo/config.toml` for both MSVC and GNU
- Config loads from `~/.wsl-tui/config.toml` with `StorageMode::Auto` default
- `WSL_TUI_STORAGE` env var overrides config file setting
- First run creates config dir and writes fully commented default config.toml
- All public functions have unit tests
- Release profile configured for binary size optimization
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>

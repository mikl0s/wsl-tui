---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - wsl-core/src/storage/mod.rs
  - wsl-core/src/storage/libsql.rs
  - wsl-core/src/storage/json.rs
  - wsl-core/src/lib.rs
  - wsl-core/src/error.rs
autonomous: true
requirements:
  - FOUND-02
  - FOUND-03
  - DX-04

must_haves:
  truths:
    - "A smoke test creates a libsql table, inserts a row, and queries it cleanly without a stack overflow on Windows"
    - "When libsql initialization fails in auto mode, JsonBackend activates transparently"
    - "Both backends implement the same StorageBackend trait with identical method signatures"
    - "`backend_name()` returns the correct string for TUI status bar consumption"
    - "When open_storage(Auto) succeeds with libsql and a data.json exists from a prior JSON run, `migration_available` flag is true in the returned result"
  artifacts:
    - path: "wsl-core/src/storage/mod.rs"
      provides: "StorageBackend trait, BackendKind enum, StorageResult struct, open_storage factory with migration detection"
      exports: ["StorageBackend", "BackendKind", "StorageResult", "open_storage"]
    - path: "wsl-core/src/storage/libsql.rs"
      provides: "LibsqlBackend implementing StorageBackend with async_trait"
      contains: "impl StorageBackend for LibsqlBackend"
    - path: "wsl-core/src/storage/json.rs"
      provides: "JsonBackend implementing StorageBackend with file-based JSON"
      contains: "impl StorageBackend for JsonBackend"
  key_links:
    - from: "wsl-core/src/storage/mod.rs"
      to: "wsl-core/src/storage/libsql.rs"
      via: "open_storage factory instantiates LibsqlBackend"
      pattern: "LibsqlBackend::open"
    - from: "wsl-core/src/storage/mod.rs"
      to: "wsl-core/src/storage/json.rs"
      via: "open_storage factory falls back to JsonBackend"
      pattern: "JsonBackend::open"
    - from: "wsl-core/src/storage/mod.rs"
      to: "wsl-core/src/config.rs"
      via: "BackendKind maps to StorageMode from config"
      pattern: "BackendKind"
---

<objective>
Implement the dual storage backend system: libsql as the primary embedded database with JSON as a transparent fallback.

Purpose: Deliver the storage abstraction that all subsequent phases use for data persistence. Prove libsql works on Windows with the stack size fix from Plan 01. Establish the trait-based pattern so calling code never knows which backend is active.

Output: StorageBackend trait with two implementations, factory function with auto-fallback, and comprehensive tests including the libsql smoke test from Phase 1 success criteria.
</objective>

<execution_context>
@C:/Users/mikkel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mikkel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: StorageBackend trait and LibsqlBackend implementation with smoke test</name>
  <files>
    wsl-core/src/storage/mod.rs
    wsl-core/src/storage/libsql.rs
    wsl-core/src/lib.rs
  </files>
  <action>
    Create `wsl-core/src/storage/mod.rs`:
    - Define `StorageBackend` trait with `#[async_trait::async_trait]`:
      - `async fn execute(&self, sql: &str, params: Vec<libsql::Value>) -> Result<u64, CoreError>` (returns rows affected)
      - `async fn query(&self, sql: &str, params: Vec<libsql::Value>) -> Result<Vec<libsql::Row>, CoreError>` (returns rows)
      - `fn backend_name(&self) -> &'static str` (for TUI status bar per locked decision)
    - Define `BackendKind` enum: `Auto`, `Libsql`, `Json` (maps from config::StorageMode)
    - Implement `From<StorageMode> for BackendKind`
    - Re-export `StorageBackend`, `BackendKind`

    NOTE on Row type: Since libsql::Row and serde_json don't share a common type, define a minimal `StorageRow` wrapper that both backends can return. Each row is a `Vec<StorageValue>` where `StorageValue` is an enum with `Null`, `Integer(i64)`, `Real(f64)`, `Text(String)`, `Blob(Vec<u8>)` variants. This keeps the trait independent of libsql types.

    Update the trait to use `StorageRow` and `StorageValue` instead of `libsql::Row` and `libsql::Value`:
    - `async fn execute(&self, sql: &str, params: Vec<StorageValue>) -> Result<u64, CoreError>`
    - `async fn query(&self, sql: &str, params: Vec<StorageValue>) -> Result<Vec<StorageRow>, CoreError>`

    Create `wsl-core/src/storage/libsql.rs`:
    - `LibsqlBackend` struct holding `libsql::Connection` (and `libsql::Database` to keep it alive)
    - `LibsqlBackend::open(config_dir: &Path) -> Result<Self, CoreError>`:
      - Build database at `config_dir.join("wsl-tui.db")` using `libsql::Builder::new_local`
      - Connect and return
    - Implement `StorageBackend` for `LibsqlBackend`:
      - `execute`: Convert `StorageValue` params to libsql params, call `conn.execute()`
      - `query`: Convert params, call `conn.query()`, convert `libsql::Row` to `StorageRow`
      - `backend_name`: return `"libsql"`
    - Write tests:
      - `test_libsql_smoke()` — THE critical smoke test: create table, insert row, query it back. Uses `:memory:` database. This proves the /STACK:8000000 fix works.
      - `test_libsql_backend_name()` — returns "libsql"
      - `test_libsql_execute_and_query()` — create table, insert, select, verify values

    Update `wsl-core/src/lib.rs`:
    - Add `pub mod storage;`
  </action>
  <verify>
    Run `cargo test -p wsl-core -- storage::libsql` — smoke test passes (no stack overflow).
    Run `cargo clippy --workspace -- -D warnings` — zero warnings.
  </verify>
  <done>
    StorageBackend trait defined with execute, query, backend_name. LibsqlBackend opens a local database, converts between StorageValue and libsql types, and passes the smoke test (create table, insert, query) without stack overflow on Windows.
  </done>
</task>

<task type="auto">
  <name>Task 2: JsonBackend implementation and open_storage factory with auto-fallback</name>
  <files>
    wsl-core/src/storage/json.rs
    wsl-core/src/storage/mod.rs
    wsl-core/src/error.rs
  </files>
  <action>
    Create `wsl-core/src/storage/json.rs`:
    - `JsonBackend` struct holding file path (`PathBuf`) and in-memory state (`Arc<Mutex<JsonData>>`)
    - `JsonData` struct: `HashMap<String, Vec<Vec<StorageValue>>>` — keys are table names, values are rows
    - `JsonBackend::open(config_dir: &Path) -> Result<Self, CoreError>`:
      - File path: `config_dir.join("data.json")`
      - If file exists, load and deserialize with `serde_json`
      - If not, start with empty state
    - Implement `StorageBackend` for `JsonBackend`:
      - `execute`: Parse simple SQL (CREATE TABLE, INSERT, DELETE) via basic string matching — this is NOT a full SQL parser, just enough to handle the app's own queries. For Phase 1, support:
        - `CREATE TABLE IF NOT EXISTS {name} (...)` — create empty table entry
        - `INSERT INTO {name} ... VALUES (...)` — append row
      - `query`: Parse `SELECT ... FROM {name}` — return all rows from table
      - `backend_name`: return `"json"`
    - Write to disk after each mutation (execute call) by serializing full state to JSON
    - Write tests:
      - `test_json_smoke()` — create table, insert, query (mirrors libsql smoke)
      - `test_json_backend_name()` — returns "json"
      - `test_json_persistence()` — write, drop, reopen, verify data persists (use tempdir)

    Update `wsl-core/src/storage/mod.rs` — add `StorageResult` struct and `open_storage` factory function:

    Define `StorageResult` struct:
    - `backend: Box<dyn StorageBackend>` — the active storage backend
    - `backend_name: &'static str` — "libsql" or "json", consumed by the TUI status bar (locked decision)
    - `migration_available: bool` — true when libsql is active AND `data.json` exists from a prior JSON-fallback run (locked decision: offer migration when libsql becomes available after running on JSON)

    `pub async fn open_storage(config_dir: &Path, kind: BackendKind) -> Result<StorageResult, CoreError>`:
    - `BackendKind::Libsql` → `LibsqlBackend::open()`, check for `data.json` existence, return `StorageResult { backend, backend_name: "libsql", migration_available: data_json_exists }`
    - `BackendKind::Json` → `JsonBackend::open()`, return `StorageResult { migration_available: false, .. }`
    - `BackendKind::Auto` → try `LibsqlBackend::open()` first; on success, check for `data.json` existence and set `migration_available` accordingly; on failure, fall back to `JsonBackend::open()` with `migration_available: false` — transparent fallback per locked decision

    The `migration_available` flag establishes the data contract for the migration prompt UI (deferred to Phase 2 when the TUI has a real status bar). The detection logic is: `config_dir.join("data.json").exists()` checked when the active backend is libsql. No actual data migration happens in Phase 1 — only the detection and the flag in the return type.

    Update `wsl-core/src/error.rs` if additional error variants are needed for storage operations.

    Write integration tests:
    - `test_open_storage_auto_libsql_succeeds()` — auto mode opens libsql (using tempdir), migration_available is false (no prior data.json)
    - `test_open_storage_json_explicit()` — json mode opens json, migration_available is false
    - `test_open_storage_factory_returns_backend_name()` — verify backend_name matches backend.backend_name()
    - `test_open_storage_migration_detection()` — create a tempdir, write a dummy `data.json` file, then call `open_storage(Auto)` — since libsql succeeds AND data.json exists, `migration_available` must be true (locked decision: detect when migration is available after running on JSON fallback)

    Run full test suite.
  </action>
  <verify>
    Run `cargo test -p wsl-core -- storage` — all storage tests pass (including migration detection test).
    Run `cargo clippy --workspace -- -D warnings` — zero warnings.
    Verify the factory function returns the correct backend name for auto, libsql, and json modes.
    Verify `migration_available` is true when libsql opens successfully and data.json exists, false otherwise.
  </verify>
  <done>
    JsonBackend persists data to `~/.wsl-tui/data.json`, implements the same trait as LibsqlBackend. The `open_storage` factory tries libsql first in auto mode and falls back transparently to JSON. Both backends return their name via `backend_name()` for TUI status bar display. The `StorageResult.migration_available` flag detects when prior JSON data exists alongside a now-active libsql backend, establishing the data contract for Phase 2's migration prompt UI. All storage tests pass.
  </done>
</task>

</tasks>

<verification>
1. `cargo test -p wsl-core -- storage` — all tests pass
2. libsql smoke test creates table, inserts, queries without stack overflow
3. JSON backend persists to disk and survives round-trip (write + reopen + read)
4. `open_storage(Auto)` tries libsql first, falls back to JSON on failure
5. `backend_name()` returns correct string for each backend
6. `migration_available` is true when libsql active and data.json exists, false otherwise
7. `cargo clippy --workspace -- -D warnings` — zero warnings
</verification>

<success_criteria>
- LibsqlBackend passes smoke test on Windows (create table, insert, query — no stack overflow)
- JsonBackend reads/writes `data.json` and survives reopen
- `open_storage` factory handles Auto (fallback), Libsql (explicit), Json (explicit) modes
- Both backends implement identical `StorageBackend` trait
- `backend_name()` returns correct identifier for TUI status bar consumption
- `StorageResult.migration_available` detects prior JSON data when libsql is active (locked decision: offer migration)
- All public functions have unit tests
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
